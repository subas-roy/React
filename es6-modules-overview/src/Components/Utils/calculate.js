const add = (first, second) => {
  return first + second;
}

const multiply = (first, second) => {
  return first * second;
}

const devideFirstNumberBySecondNumber = (first, second) => {
  return first / second;
}

export {add, multiply, devideFirstNumberBySecondNumber} // export multiple item as object

// export default add;
// const calculator = {add: add}
// export default calculator;
